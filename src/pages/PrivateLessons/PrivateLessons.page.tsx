import React from "react";

import styles from "./PrivateLessons.module.css";

type Props = {};

const PrivateLessons = (props: Props) => {
    return <div>PrivateLessons</div>;
};

export default PrivateLessons;
